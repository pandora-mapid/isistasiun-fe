# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Project state

This started as a bare `create-next-app` scaffold and now implements **Isi Stasiun**, a 5-screen WebGIS proposal UI (Beranda `/`, Peta `/peta`, Insight `/insight`, Metodologi `/metodologi`, Rekomendasi `/rekomendasi`), recreated from a Claude Design hi-fi mockup (`Isi Stasiun Hifi v2.dc.html`, pulled via the design-system sync tool from the "Proposal Aplikasi Pemetaan Stasiun" project). Peta is a fully interactive MapLibre map over mock data (ROADMAP Fase 0 and Fase 1 are both done). Beranda has since been redesigned onto a second, deliberately different visual system (see "Two design systems" below) and now reads the same live mock data Peta does; **Insight followed the same migration** — it is on the paper system and reads live mock data through its own `InsightData` provider. **Metodologi and Rekomendasi have since been migrated onto the paper system too, but as a restyle only** — their layout and content are unchanged, they are still static illustrative mockups (no live data), and Rekomendasi keeps its old "Stasiun A/B/C" / "tiga simpul" copy (real 2-station wiring is a separate future task). Peta is the last screen still on the original slate system, and it is left there deliberately — it is the interactive map screen, not a restyle candidate.

- `components/NavBar.tsx` — a single `NavBar` (bordered row) used identically on all 5 screens, including Peta; it hardcodes the 5 routes and highlights the active one via an `active` prop — no `usePathname()`. Internally split into `Brand` (the "IS" mark) and `NavLinks` (the route pills), but those two are not exported — everything renders through `NavBar`. An earlier `FloatingNavBar` variant (a glass pill overlaid on the Peta map) was removed in favor of this single component so all 5 screens look consistent; don't reintroduce a second nav variant without checking with the user first. Both its colours (`--nav-brand-bg` etc.) and, since round 5, its **shape** (`--nav-radius`/`--nav-margin`/`--nav-bg`/`--nav-shadow`/`--nav-border-w`/`--nav-brand-radius`) travel through CSS custom properties whose `:root` defaults reproduce the original literal styling exactly; only `.paper-canvas` currently overrides them, turning the same markup into Beranda's floating pill bar. Extend this seam for a new per-screen look before reaching for a second component.
- `components/PetaScreen.tsx` — the only client component (`"use client"`); owns all of Peta's interactive state (tab, layers panel, category/catchment/slot filters, selected point, transparency modal, copilot reveal) and loads the data once via `usePetaData`. Its `page-canvas` is a column flexbox pinned to `100vh` (`NavBar` on top, the map `flex: 1` below) so the map always fills the remaining viewport with no page-level scroll. The illustrative basemap SVG that used to live here was replaced by `MapCanvas`; the floating panels still sit over it, absolutely positioned. **Every figure in the brief panel now comes from the mock analytics** through `lib/analytics/select.ts` — nothing in the JSX computes a number of its own, so the map and the panel cannot disagree. The one canned area left is the "Tanya Data" (copilot) tab, which is Fase 3.6.
  - **Every interactive control in the panels is a real `<button>`.** They were all `<div onClick>` at first — indistinguishable on screen, and unreachable by Tab or Enter, which made `/peta` unusable without a mouse. They keep their original look through `.btn-reset` (see the design-tokens note below) and carry `aria-pressed`/`aria-expanded`. Don't convert them back to `div`s for styling convenience; `tests/peta.spec.ts` drives three of them from the keyboard. Layer rows with no data use `aria-disabled`, not `disabled`, so the "belum ada data" tag stays readable — Playwright refuses to click them, which is the correct signal.
  - Selected point is stored as `number | null | undefined`: `undefined` means "user hasn't chosen", which falls back to the biggest-gap point; `null` means the user explicitly cleared it. Collapsing those two into one value makes the close button impossible to implement — the default would instantly refill it.
  - Three layer rows are live; only *Indeks sewa* and *Event* are disabled with a "belum ada data" tag, and they genuinely have no mock data. `LAYER_GROUPS` in `lib/map/config.ts` decides which rows are live — add a key there and the row activates itself. Do not mark a row "belum ada data" without checking the mock first: *Kategori hilang* and *Arus pintu* carried that tag for a while although `by_category` and `variables.F` had existed since Fase 1.
  - A *Potensi belanja* ring layer used to exist and was removed: it drew potensi and gap on two independent radius scales, so the spacing between ring and circle — the thing it was meant to communicate — varied between points whose real ratio was identical. Don't reinstate it without a zero-anchored, area-proportional scale shared with the gap circle.
  - The legend deliberately omits *Sampel tipis* even though the map still draws those points differently (white fill, grey ring). That is a team decision — the brief panel states "Tidak diestimasi" with its reason — and it is a knowing exception to the rule that every map symbol earns a legend entry. Don't "fix" it back without asking. The rendering itself must stay: without it a point with no data reads as the smallest gap, which is the opposite of the truth.
  - A *Kategori hilang* badge layer was also tried and removed. The brief panel already lists the categories by name with demand share and store count, and each row filters the map — a bare count on the map was strictly less informative. Before drawing anything on the map that the panel already explains, check whether the map version can carry the same meaning; usually it cannot.
- `components/beranda/` — the Beranda-only leaf components. `BerandaData` is a client context provider that calls `usePetaData()` **once** and hands out picked figures; `HeroPeta`, `Persamaan`, `PerbandinganSimpul`, `RelStatistik`, and `Jejak` read it. `app/page.tsx` itself stays a **server** component — the client boundary lives in the leaves, and the sections passed as `children` are still server-rendered. `HeroPeta` returns one block (the rounded map tile with a stat card overlapping its bottom-right corner), placed as the right column of the hero's two-column grid — a round-5 `BadgeCincin` decorative ring badge lived here too but was cut in round 6; don't resurrect it without checking first. Round 10 replaced `KartuSimpul` (3 station cards) with `PerbandinganSimpul` and rewrote `RelStatistik`; `RangeBar`/`SlotSparkline` were deleted then. **Round 11 gave each numbered section its own composition** (the user kept rejecting "one pattern applied to all → monotone"), then **round 12 renumbered so the ink-band `Persamaan` counts as a section too**: 1 Duduk perkara = one enormous `Rp X jt` figure (`RelStatistikAngka`, framed top-and-bottom: a label + hairline above, a P10–P90 range bar with the median tick at its true position below — the figure's own caption flows to exactly the figure's width via a `width:0;min-width:100%` wrapper) + two quoted facts as marginal notes (`RelStatistikKutipan`, pulled up `-var(--s4)` so `KepalaBab`'s rule doubles as its first row's border), header and h2 RIGHT-aligned (`KepalaBab align="right"` — which also flips `KickerBernomor` so the number sits at the far edge); 2 Instrumen = the `Persamaan` F×E×C×V band; 3 Cara datanya = photo + corner-overlap stat card; 4 Yang sudah dicacah = 3 staggered small-multiple cards on one shared 0..max scale (`PerbandinganSimpul`), title LEFT; 5 Dari peta ke keputusan = one gold highlight card beside a numbered list, title full-width; 6 Untuk siapa = 3 diagonally-stacked overlapping cards, title RIGHT. The section number lives in `KickerBernomor` (shared by `KepalaBab` and `Persamaan`) — a bare mono `{n}` (no em-dash since round 13) as a large display numeral, `clamp(44px,6vw,84px)`, sitting **outside** the kicker pill in `--ink-faint` (large but reads as background, and auto-lightens on the ink band — deliberately not the round-10 solid-giant numeral the user rejected). `KickerBernomor align="right"` puts the pill first and the numeral last, so the number anchors the far (page) edge. `.kartu` in `globals.css` is the shared "creative card" family — `--surface` fill, 1px `--rule` border, uniform `--r-md` rounding, **no shadow** — used by every overlap/stagger card so the variety still reads as one system.
  - **Beranda's figures are picked, never computed.** Its station rows used to be hardcoded as "Stasiun A/B/C" with typologies and door counts that *contradicted* `stations.json` (Stasiun C was claimed to have 5 doors; the data says 3) — the landing page disagreeing with the product behind it. Everything now routes through `lib/analytics/select.ts`, and where the payload has no figure at a given level, the page picks a real one rather than deriving: a station row shows its **largest-gap point**, not a sum of its points, because summing invents a number nobody measured (ROADMAP §9.4). `totalMetricFor()` exists for exactly this reason — Beranda talks in "per hari", so it reads `total`, and leaves `variables`/`arus` null because F × E × C × V is only ever counted per slot. `tests/beranda.spec.ts` locks that the station names "Manggarai"/"Sudirman" render and "Stasiun A" never does.
- `components/paper/` — the shared "laporan instrumen" chrome: `KickerBernomor` (the large `--ink-faint` display numeral + kicker pill), `Lencana` (the `.eyebrow-chip`), `Judul` (the one `<h2>`), `KepalaBab` (kicker + judul + hairline; `halfRule` cuts the rule to the left half of a `1fr 1fr`/`--s5`-gap grid), `Kepala` (the numeral-free alternative — kicker `.eyebrow` + modest `<h2>` at `clamp(25px,2.5vw,32px)` + right-aligned `catatan` note; born as a local in `app/insight/page.tsx`, lifted here when Metodologi needed the same head). These take **no data**, which is what separates `paper/` from `beranda/`+`insight/`. **`KepalaBab`/`Judul` are Beranda-only** (numbered magazine sections); **`Kepala` is used by Insight + Metodologi + Rekomendasi**; `Lencana` by all four. Extend this folder before duplicating a primitive or forking the nav.
- `components/insight/` — the Insight leaves. `InsightData` is a client provider that calls `usePetaData()` **once**, scopes the payload to Manggarai + Sudirman (`station_id` 1 & 2 — the 3rd mock station is a placeholder), and picks every figure through `lib/analytics/select.ts`; `InsightBagian.tsx` holds the client leaves (`HeroKartu`, `KpiStrip`, `PeringkatPintu`, `BatangBersarang`, `KolomSlot`, `InstrumenSlot`, `DaftarTemuan`, `MatriksKategori`, `SorotanKategori`, `JejakInsight`). `app/insight/page.tsx` stays a **server** component wrapping `<InsightData>`. Nothing in the JSX computes a number (bar/column pixel positions excepted) — same rule as Beranda; the two ratios the provider does derive (`capturePersen` = tertangkap/potensi, and the `strukTerbaca` sum of `evidence.struk_terbaca`, a coverage count not an invented rupiah) are the deliberate precedent, nothing else. The category matrix's Terisi/Kurang/Kosong buckets `gerai_count` against `AMBANG_GERAI` (3), not an invented `n < 30`; its "Menahan" column is the category's slot-`pagi` `gap.p50` at the Manggarai column point.
  - **Insight's composition borrows from Metodologi/Rekomendasi, not Beranda.** It shares the design *system* — `.paper-canvas` (palette, type scale, `hanging-punctuation`, the floating nav pill), `.fig` figures, `.reveal` scroll — and, since round 4, their **full-width density**: **no `.wrap`** (dropped — `<section>`s take `padding: 0 var(--page-x) var(--s4)`, content follows the viewport like the two old screens), ~`--s4` between sections, each section a grid of 2–3 cards. Hero grid (`1fr clamp(340,30vw,420)px`, `align-items:end`) with text left + a dark `.kartu.ink-band` stat card right (the page's only dark element — `background:var(--ink)` set inline so `.kartu`'s surface fill doesn't win), then a `KpiStrip` (5 cells) and sections of a local `Kepala` helper (`.eyebrow` kicker + modest `<h2>` at `clamp(25px,2.5vw,32px)` + a right-aligned `catatan` note — **no numeral**). Cards are **flat, no shadow** — `.kartu` white cards + inline `{background: <tint>; border-radius:var(--r-md); padding:var(--s3)}` fills — and their fill **cycles the majority of Beranda's palette** (`--tile-sky/-mint/-violet/-rose` + `--data-wash` on the KPI strip, `--data-wash` on the two data cards and the matrix header/odd-rows, `--tile-sky` on the slot chart, `--tile-violet` on the category sorotan, `--field-wash` on the thin-sample inset, one `.ink-band`) over **one uniform `--paper` ground that never changes colour** — so it is *not* a full-bleed band and *not* "pemisahan halaman". **`--paper-2` is deliberately not a card fill here** — round 4's first pass tried it on the matrix and the KPI strip's fifth cell and the user flagged both as "too close to the background"; anything tinted must read clearly against warm `--paper`, so the cool `--data-wash` (a data table, semantically) took over. The analytics content: `PeringkatPintu` (ranked stacked-bar list of all 7 scoped points by daily gap — `/rekomendasi` "Urutan prioritas" idiom), `InstrumenSlot` (F×E×C×V readout for the peak slot — `/metodologi` equation-card idiom, minus the ink band), the matrix "Menahan" column, capture % and P10–P90 range on the hero-door card. **No `--tile-*` bands, no `KickerBernomor` numerals, no `.card`/`.bento-tile` shadows** — the round-1 "sectioned magazine", round-2 "flowing essay", and round-3 single-panel-per-section were all rejected. Figure colour still follows Beranda's *usage* regardless of the tile under it: `--data` only for graphical marks + instrument readouts (F/E/C/V, demand %, capture %, the "Menahan" gap) + the hero-card figures (auto `--data-mid` on `.ink-band`); `--ink`/`--ink-2` for headings + the "Kesenjangan" conclusion figure + plain counts in the KPI strip; `--field` ochre only for the thin-sample `--field-wash` inset + its `tag-field` chip; `--brand` gold never on a figure.
  - `tests/insight.spec.ts` locks the paper-canvas class, the `.reveal` opacity guard, reduced-motion, an h1 matching `/dari dua simpul/` + an h2 exactly "Pagi menahan kesenjangan terbesar.", "Manggarai"/"Sudirman" present with "Stasiun A"/"Stasiun C"/"tiga simpul"/`n < 30` absent, the real `3 gerai × 2 blok` rule shown, and a picked figure (`Rp 2,9 jt`) + `pipeline mock-` provenance. **If you reword those two headings, update the spec.**
- `components/ImagePlaceholder.tsx` — stands in for real station/survey photography.
- **Two design systems live in `globals.css` side by side, and that is deliberate.** The original one (`--gray-*`/`--accent-*`, Tailwind slate verbatim, `border-radius: 12px` on everything) still drives one screen (Peta) — deliberately, since it is the interactive map. Beranda, Insight, Metodologi and Rekomendasi run on a second one, scoped by the `.paper-canvas` class — warm paper, real type/space/radius scales. **Do not "unify" them by editing the old tokens**: `body`, `.page-canvas`, and Peta read those, so changing them repaints the whole app. Migrate a screen at a time by adding `.paper-canvas` and rewriting its markup — Insight is the worked example (`components/paper/` extracted, `components/insight/` added, `globals.css` untouched); Metodologi and Rekomendasi are the cheap version of the same move (a pure restyle — `page-canvas` → `page-canvas paper-canvas`, slate hex → paper tokens, `.mono` → `.fig`, headers → `Kepala`, dark `#0F172A` cards → `.ink-band`, `#EEF2F6` panels → cycling tints; layout and content untouched, still no live data). **The `.paper-canvas` *system* (palette, tokens, `.fig`, nav, `.reveal`) is shared; the *composition* is per-screen.** "Each section its own composition / pastel `--tile-*` section backgrounds / `KickerBernomor` display numerals" is **Beranda's** answer (a landing page that fought monotony — see the round history below), not a paper-system requirement. **Insight uses the full-width dense card grid Metodologi/Rekomendasi established instead** — no `.wrap` cap, `<section>`s at `padding: 0 var(--page-x) var(--s4)`, each a 2–3-card grid; flat `.kartu`/inline-fill panels whose backgrounds **cycle the majority of Beranda's palette** (`--tile-sky/-mint/-violet/-rose`, `--data-wash`, `--field-wash` — not `--paper-2`, which reads too close to the ground) over one **uniform `--paper` ground that never changes colour**, plus one dark `.ink-band` hero card; modest section headers; a KPI strip; ranked per-door bars and an F×E×C×V readout for the "analytics" feel. **No full-bleed bands** (the ground is constant — that is the line between "cycling tinted cards" and "pemisahan halaman"). (Three earlier Insight compositions — Beranda-style sectioned, narrow flowing-essay, and a sparse single-panel-per-section grid — were built and rejected first.) A migrated screen picks whichever fits its content.
  - Beranda's own visual language has itself been through three eras. Rounds 1–4 built an *editorial* system — hairlines instead of card fills, sudut nyaris tajam, one single deliberate shadow exception (the hero map card). Round 5 pivoted to a *modern* system (radius/shadows became the default, pill buttons/nav, Inter 800/900 headlines, `--brand` gold as the one new accent). Round 6 kept round 5's surface language but rebuilt the *frame*: the page had stayed a vertical stack of `wrap > g12 > Rubrik (sticky column) + content` sections even after round 5's reskin, and the user called that out specifically — a modern skin on an editorial-report skeleton. `Rubrik()` is gone; almost every section is now a `.bento` grid of mixed-size `.bento-tile`s (`--r-xl` radius, uniform across the whole page) with a small rotating pastel palette (`--tile-sky`/`-violet`/`-mint`/`-rose`) for tile backgrounds — decorative only, deliberately a *second, independent layer* from the meaningful trio (`--data`/`--field`/`--brand`); a tile's background hue never changes what color the figures inside it are allowed to be. Sections 03 (Persamaan) and 09 (closing ink-band) went untouched through all three eras — the user flagged both, twice, as the parts that already work; a later round did rework `Persamaan`'s header (flex row, not `.g12`, so the h2 can `nowrap`; intro paragraph shortened; em-dashes out of its rendered text) but the F×E×C×V band itself still stands. **Rounds 7–11 then walked the bento grid *back* for Beranda specifically**: after the user kept calling the tile grid "boxes with text, vibe-coded", round 10 unified every section on one `KepalaBab` header + hairline layout — which the user then rejected *as monotone*. Round 11's rule (still current): **the small system stays fixed (one 96px rhythm, one type scale, one `.kartu` card family — uniform `--r-md` rounding, no shadow — one large `{n}` display numeral + KICKER-pill header via `KickerBernomor`, pastel section backgrounds, mono figures, hairlines-not-shadows) and every section's *composition* differs** — enormous-figure / photo-overlap / staggered small-multiples / list+highlight / stacked-cards, with the title alternating left↔right down the page. Round 12 pulled the section number out of the kicker pill and enlarged it, and made `Persamaan` a numbered section (so the count is now 1–6); round 13 dropped its em-dash and blew the numeral up to display size. `.bento`/`.bento-tile` still drive Peta; on Beranda, Insight, Metodologi and Rekomendasi they're superseded by `.kartu` + inline tinted fills. Don't assume a comment describing "hairline, no shadows", "3-up bento tiles", "single sharp corner" or "full-width KepalaBab everywhere" still holds — check the component's current definition.
  - Colour carries meaning and the rule is load-bearing: `--data` (blue, **identical** to `GAP_RAMP` in `lib/map/style.ts`) is only ever numbers, the map, and data marks; `--field` (ochre) is only ever fieldwork — the survey, the counters, the photos; `--brand` (warm gold, added in round 5) is only ever brand chrome — primary buttons, the active nav pill, decorative badges — **never** a number or a map mark; `--ink` is argument; `--paper` is ground. Three colours, three fixed meanings; using one for a fourth purpose dilutes all three. (Round 1–4's primary buttons were ink rather than blue for the same reason blue is reserved now — round 5 gave that job to gold instead of loosening the rule.)
  - `.mono` is untouched and still means "Inter with tabular figures". The new system uses `.fig` (real monospace) instead. Beranda, Insight, Metodologi and Rekomendasi are fully on `.fig`; `.mono` is still load-bearing on Peta, where several panels have fixed-width numeric columns that would break under different digit metrics — migrate it per screen, not globally.
- All 5 pages are laid out inside `.page-canvas` (defined in `globals.css`), now fluid at `width: 100%` though authored at 1440px. Beranda uses `clamp()` + `minmax(0, Nfr)` columns inside `.wrap` (1400 max) and holds to ~1100px; Insight, Metodologi and Rekomendasi drop `.wrap` entirely and run full-width — `minmax(0, Nfr)` section grids at a `--page-x` gutter, no max-width, breaking below ~1100px just like Peta (fixed grid columns). Full responsiveness is a proposal promise that remains unbuilt (`ROADMAP.md` §6).
- `.btn-reset` in `globals.css` strips the browser's default button chrome (`all: unset`) so a `<button>` can wear `.lyr`, `.chip`, `.pill`, `.row`, or `.ic` and look exactly like the `div` it replaced, plus a `:focus-visible` ring. **It must stay above those rules in the file.** They are all single-class selectors, so the later one wins — move `.btn-reset` below them and its `all: unset` deletes the very styling it exists to preserve, silently.
- Design tokens (the neutral/accent color scale, `.b`/`.bp`/`.bs`/`.bw` buttons, `.chip`, `.pill`, `.glass`, `.k`/`.cap` labels, `.row`, `.lyr`, `.dot`, `.mono`) are ported 1:1 from the source `.dc.html` into `globals.css` — most inline styles in the pages use literal hex/px values matching that source rather than Tailwind utility classes or the `:root` custom properties, so a rebrand means editing values across all 5 files, not just `:root`. Always reference the font via `var(--font-inter)`, never a literal `'Inter'` string — next/font hashes the actual generated font-family name, so a literal string silently falls back to the system font.
- Several buttons across the app are intentionally inert (styled, no `onClick`) because they'd need a real backend: PDF/brief export, "Bandingkan", "Tabel atribut". This is expected, not a bug to fix reflexively — confirm with the user before wiring them to anything.

Note the `@AGENTS.md` import above: `AGENTS.md` is auto-generated and rewritten by `next dev` itself (see `node_modules/next/dist/server/lib/generate-agent-files.js`). It warns that this Next.js version (16.3.2) has breaking API/convention changes relative to older training data, and points to `node_modules/next/dist/docs/` as the authoritative reference — check there before assuming App Router behavior from prior knowledge. If it reappears as an uncommitted diff after `next dev` runs, that's expected; commit it to keep the tree clean rather than deleting it.

## Commands

Run from `fe/` (the actual project root — the repo lives at `D:\Lomba\MAPID\fe`, not the outer `D:\Lomba\MAPID`):

```bash
npm run dev        # start dev server (http://localhost:3000)
npm run build      # production build
npm run start      # serve the production build
npm run lint       # ESLint (flat config, eslint.config.mjs)
npm test           # Playwright: pure-logic + style-spec + browser smoke tests (88)
npm run test:style # just the fast, browser-free layer validation
```

`predev`/`prebuild` run `scripts/copy-maplibre-worker.mjs` automatically — see the MapLibre worker note under Architecture.

**Testing** is Playwright (`tests/`, config in `playwright.config.ts`), added because three separate map bugs each produced a blank map with *no error message at all* and sailed past both typecheck and lint:

- `tests/beranda.spec.ts` — the landing page. Asserts the hero map actually **draws** features and is genuinely non-interactive (`dragPan`/`scrollZoom` disabled, no navigation control), that the station cards say "Manggarai" rather than "Stasiun A" — locking the data-contradiction fix — and one guard worth naming: **no `.reveal` element may be transparent before you scroll**. The first version of the scroll reveal faded from `opacity: 0` via `animation-timeline: view()`; the page looked perfect while scrolling and captured as *hero and footer only, everything between blank*. The keyframes are now transform-only for that reason. Don't reintroduce opacity there.
- `tests/insight.spec.ts` — the Insight page, templated from `beranda.spec.ts` minus the map. Locks the `.page-canvas.paper-canvas` wrapper, the same **no `.reveal` transparent before scroll** guard, reduced-motion killing every reveal animation, the migrated-content facts ("Manggarai"/"Sudirman" present, "Stasiun A"/"Stasiun C"/"tiga simpul"/`n < 30` absent, the real `3 gerai × 2 blok` rule shown), and a picked figure (`Rp 2,9 jt`) + `pipeline mock-` provenance so the page can't silently fall back to hardcoded numbers.
- `tests/metodologi.spec.ts` — the Metodologi page, same template minus the data asserts (it has no live data). Locks the `.page-canvas.paper-canvas` wrapper, the nav-active state, the h1 + a section h2, the `F × E × C × V` equation ("Persamaan potensi" + "Kesenjangan"), the **no `.reveal` transparent before scroll** guard, and reduced-motion killing every reveal — so the paper-system migration can't silently regress.
- `tests/rekomendasi.spec.ts` — the Rekomendasi page, same template as `metodologi.spec.ts` (restyle only, no live data). Locks the `.page-canvas.paper-canvas` wrapper, the nav-active state, the h1 + a section h2 ("Tiga rekomendasi utama"), the **no `.reveal` transparent before scroll** guard, and reduced-motion killing every reveal. Deliberately asserts **no** data strings and does **not** guard against "Stasiun A" — that copy is legit illustrative content here.
- `tests/select.spec.ts` — `lib/analytics/select.ts`, the file that decides *which number* every panel and every circle shows. No browser, no React, no MapLibre — it's all pure functions, so it's the cheapest thing in the repo to test and the most expensive to get wrong: a mistake here surfaces as a **wrong number in two places at once**, not as a broken page, so neither the style validator nor a screenshot would catch it. Locks in the rules that are easy to "simplify" away: a missing category falls back to the slot aggregate (not to zero), a missing slot reads "tidak diestimasi" (not Rp 0), thin samples never shift the scale, and `domainOf` always returns a domain with width.
- `tests/scale.spec.ts` — the distance scale bar arithmetic (`lib/map/scale.ts` + `jarak()`). Also browser-free. The old hardcoded "0 150 300 m" bar never *looked* wrong, it was only ever *numerically* wrong — a class of bug no visual test can see.
- `tests/map-style.spec.ts` — validates every layer spec against MapLibre's official validator. No browser, ~1s. Catches illegal expressions (e.g. `feature-state` inside `filter`, `["zoom"]` nested inside `case`).
- `tests/peta.spec.ts` — real Chromium. Asserts features are actually **rendered** (`queryRenderedFeatures`), not merely that the page loaded, and that each Fase 1 interaction changes what is drawn or written — slot and category filters change the attached values, clicking a point sets `terpilih`, layer rows flip `visibility`, and the legend's upper bound moves with the data. Both of the bugs described in the Architecture notes above were caught here and nowhere else.
  - Three of its tests drive the panels **from the keyboard** (focus + Enter + Esc) rather than by clicking, because that is the only way the `<div onClick>` regression would ever show up — a mouse click works fine either way.
  - When a test needs to click a point, remember `map.project()` returns **canvas-relative** coordinates while `page.mouse` takes **viewport** coordinates, and the canvas sits below the NavBar — add the canvas `getBoundingClientRect()` offset, and skip points that fall under the floating panels, which would otherwise swallow the click.

Playwright reuses an already-running dev server, and starts one if there isn't one.

## Architecture

- **Next.js 16 App Router** (`app/` directory), React 19, TypeScript (strict mode).
- **Styling**: Tailwind CSS v4, configured via CSS (`@import "tailwindcss"` in `app/globals.css`) rather than a `tailwind.config.js` — there isn't one in v4's default setup. `globals.css` also carries the design tokens and shared component classes described above.
- **Path alias**: `@/*` maps to the project root (`./*`) per `tsconfig.json`.
- **Fonts**: two families, both via `next/font/google` in `app/layout.tsx` and both referenced through CSS variables — never as a literal family name, because next/font hashes the generated name and a literal silently falls back to the system font. Inter 400–900 (`--font-inter`, body/UI everywhere, and — since Beranda's round-5 pivot to a "modern" system — its display headlines too, at 800/900); JetBrains Mono 400/500/700 (`--font-mono`, figures). Instrument Serif (`--font-serif`) was loaded for Beranda's earlier editorial headlines and was removed in round 5 once nothing referenced it any more — if you're tempted to reach for a serif display face again, it'll need re-adding, not uncommenting.
- **Linting**: flat ESLint config (`eslint.config.mjs`) extending `eslint-config-next`'s `core-web-vitals` and `typescript` rule sets.
- **The map** (`/peta`): MapLibre GL JS v6, wired up in `components/MapCanvas.tsx`. Three conventions there are load-bearing and each one, when broken, yields a blank map with **no error message**:
  - **Every scale expression must survive a zero-width domain.** With the scale now derived from live data (`domainOf`), filtering to one category can leave a single estimated point, making `min === max`. `interpolate` requires strictly ascending inputs, so `amankanDomain()` in `style.ts` widens it — without that, every point disappears with no error. `tests/map-style.spec.ts` validates all three point layers against a deliberately flat domain.
  - **`feature-state` is legal in `paint`, illegal in `filter` *and in every layout property*.** `text-field` is layout, so a label can never read a number pushed through `setFeatureState`. Anything that must be *written out as text* travels instead as a feature **property** on the separate `point-label-values` GeoJSON source, which `PetaScreen` rebuilds whenever the slot or category changes (it holds one feature per observation point, so rebuilding is cheap).
- **A symbol layer added later wins collision placement over one added earlier.** MapLibre places symbols in reverse layer order. Putting `point-arus` after `point-label` made *every* point name vanish with no error at all. `LAYER_ORDER` therefore lists `pointArus` *before* `pointLabel`, so the supplementary number yields and the name survives; `tests/peta.spec.ts` locks that in.
- **`feature-state` is legal in `paint`, illegal in `filter`.** Analytics numbers reach the map via `setFeatureState`, so anything driven by them (gap colour/size, thin-sample styling) must be a paint expression. This is why thin-sample points share one layer with the rest and are distinguished by colour rather than filtered into their own layer.
  - **`["zoom"]` may only be the input to a top-level `interpolate`/`step`** — never nested inside `case`. Hence `circleRadiusExpression()` puts the zoom interpolation outermost and the thin-sample `case` inside it.
  - **MapLibre v6 ships its worker as a separate file** and derives its URL from `import.meta.url`. Under Turbopack that isn't an `http(s):` URL, so MapLibre falls back to `new Worker("")` — a worker pointing at the *page*, which silently does nothing: no tiles requested, no GeoJSON parsed. Fixed by `setWorkerUrl()` at module scope in `MapCanvas.tsx`, pointing at a copy that `scripts/copy-maplibre-worker.mjs` places in `public/maplibre/` (gitignored, regenerated on `predev`/`prebuild`). Don't hand-edit that folder.
- **Camera controls**: the basemap is OpenFreeMap **Liberty**, chosen because it shows the neighbourhood POIs that are this project's actual subject and because its `building-3d` layer extrudes buildings when the map is tilted. `?basemap=positron|voyager|bright` overrides it for comparison (`resolveBasemapUrl()`), and `tests/basemap-compare.visual.spec.ts` renders all four side by side. Tilt/rotate is MapLibre's own drag plus the compass, capped at `CAMERA.maxPitch` (60°) so the view can never end up staring at the horizon. **There are no custom map controls** — `visualizePitch: true` makes MapLibre call `resetNorthPitch()` on compass click instead of `resetNorth()`, which zeroes bearing *and* pitch without moving the centre, so a hand-rolled "flatten to 2D" button was redundant and was removed. An earlier auto-rotating "sweep" mode was also removed at the user's request; don't reintroduce either without asking.
- **Symbol layers must set `text-font` explicitly.** MapLibre defaults to `Open Sans Regular`, which OpenFreeMap does not serve — the glyph request 404s and every label vanishes with no error beyond an anonymous console line. `LABEL_FONT` in `lib/map/style.ts` pins `Noto Sans Regular`, which both OpenFreeMap and CARTO provide. Re-check this if the basemap changes again. `tests/peta.spec.ts` now fails on any 4xx/5xx response precisely to catch this class of silent breakage — note a 404 is *not* a `requestfailed`, so listening only for that misses it.
- **`MapCanvas` takes `interactive`, `fitPadding`, `borderRadius`, and `attributionPosition`; it was extended, not forked.** Beranda's hero passes `interactive: false`, which drops every built-in MapLibre handler *and* skips registering the hover/click listeners entirely — a map that swallows the scroll halfway down a landing page is a trap, and a pointer cursor over something unclickable is a promise not kept. `fitPadding` exists because the default `FIT_PADDING` reserves room for Peta's floating panels; the hero sends its own asymmetric set. `attributionPosition` (hero passes `"top-left"`) disables MapLibre's built-in credit — which can only sit bottom-right — and re-adds an `AttributionControl` elsewhere: the hero's stat card hangs out of the bottom-left corner and a legend chip hangs out of the right edge, so any bottom credit gets covered, and an unreadable credit breaks the basemap licence. The `HeroPeta` stat card is compact (`max-content`, capped ~380px — it was briefly a 440px pita, judged too long), sits on the map floor and juts ~40% past the **left** edge; the small gap-scale legend chip (`top: 24%`, juts past the **right** edge) is built from `GAP_RAMP` so it can't drift from the map.
- **Map styling lives in exactly one file**, `lib/map/style.ts` — colours, radii, opacity, layer specs. `ROADMAP.md` §1 requires visuals be changeable without touching data/logic, so don't inline colour or size values in `MapCanvas.tsx`.
- **Data access goes through `lib/data/source.ts`**, which currently reads mock files from `public/mock/` (generated, committed). Phase 2 swaps that one file for the Go API; nothing else should need to change. Shapes are in `lib/data/types.ts` and mirror `DATA_CONTRACT.md`. Note `MapCanvas` already joins geometry to analytics via `setFeatureState` even though both come from mocks — deliberately, so the Phase 2 switch to vector tiles doesn't require rewriting style expressions.
- **Nothing may derive facts from the full feature list.** `points.features` is available today only because geometry still arrives as GeoJSON; once it becomes vector tiles the browser only holds whatever is on screen. Three things used to be computed from it and were deliberately moved:
  - point labels → `loadEntrances()` (`public/mock/entrances.json`, later `GET /stations/:id/entrances`)
  - station names → `loadStations()`
  - the opening viewport → `STUDY_BOUNDS` in `lib/map/config.ts`

  Reintroducing a `points.features` loop would compile, pass tests, and then fail in Phase 2 with a misleading symptom: the map opens somewhere random and panels show `#24` instead of "Pintu 4". See `ROADMAP.md` §4.1.
- **Configuration comes from `NEXT_PUBLIC_*` env vars, referenced literally.** `NEXT_PUBLIC_API_BASE_URL` (falls back to `/mock`) and `NEXT_PUBLIC_BASEMAP_URL` (falls back to OpenFreeMap Liberty), documented in `.env.example` — which needs the `!.env.example` negation in `.gitignore` to survive the blanket `.env*` rule. Next.js inlines these by *textual* match at build time, so they must be written as `process.env.NEXT_PUBLIC_FOO` in full; routing them through a variable yields `undefined`.
- **The basemap is served through a backend proxy** so the MAPID key never reaches the browser, but `resolveBasemapUrl()` keeps a direct fallback on purpose: if the only address were the proxy, `/peta` would die whenever the backend is down, breaking `ROADMAP.md` §9's first principle. The proxy must rewrite `glyphs`, `sprite`, and `sources.*.tiles` inside `style.json` — those carry the key, and forwarding the file unchanged yields a blank basemap. `MapCanvas` now surfaces exactly that case: `sumberDayaGagal()` reads `status` and `url` off MapLibre's `AJAXError` and shows a one-time notice naming the HTTP status and host. **The timeout path alone was not enough** — it only fires when `style.load` never arrives, whereas an unrewritten `style.json` loads perfectly and then 401s on every tile, so the failure that is most likely in production was the one failure nothing reported.
  - MAPID's `basic` style ("Street Mapid") has been loaded and verified: it serves `Noto Sans Regular`, has a `building-3d` layer, and shows the same neighbourhood POIs as Liberty. That is not a coincidence — **it is derived from OSM Liberty**, so switching between them barely changes the picture.
  - It also pulls from two hosts that are neither MAPID nor ours: the sprite from `maputnik.github.io/osm-liberty` and low-zoom relief from `klokantech.github.io`. Neither carries a key, so both work unproxied — but proxying `basemap.mapid.io` alone will not capture them, and the POI icons depend on a GitHub Pages URL nobody on this project controls.
  - `mapid` is deliberately absent from `BASEMAP_CHOICES` (that file is committed and the URL carries a key); `basemapChoiceUrl()` resolves it from the env var instead, so `?basemap=mapid` only works when `.env.local` is filled in.
- **`/peta` must never sit behind a login.** `lib/data/source.ts` has a `setAuthToken()` seam that attaches `Authorization` when a token exists, but every map endpoint is free-tier by policy — `04-VALUE-PROP-AND-MONETIZATION.md` §3 requires the base layers stay open for UMKM equity (Locus Charter). Auth belongs to premium features only (`ROADMAP.md` §6.1); do not "tidy this up" by adding global route protection.
- **The data/logic/presentation split is a hard requirement, not a preference** (`ROADMAP.md` §1 — the pause between Fase 1 and 2 exists so visuals can be reworked freely). It is currently enforced by four layers, and changing a colour or a layout must never require touching the first three:
  - `lib/data/usePetaData.ts` — loads all four mock sources **once**, at the screen level. `MapCanvas` no longer fetches anything; it draws what props tell it to. Loading in both places meant the panel and the map could hold different copies of the same numbers.
  - `lib/data/dimensions.ts` — the `SLOTS` and `CATEGORIES` lists. Data keys (`pagi`/`siang`/`sore`/`malam`) are separate from screen labels (`06–09` …) and from the true counted range (`jam`, e.g. `16.00–18.59`). Don't duplicate these lists into a component; one mismatched label silently selects the wrong slot.
  - `lib/analytics/select.ts` — pure functions (no React, no MapLibre) that decide *which* number a given slot × category shows, plus `domainOf()` for the active scale. `metricFor()` never computes a derived figure — it only picks one that exists in the payload.
  - `lib/map/style.ts` — colours and sizes only. Since Fase 1.5 the gap scale follows the data, so `Domain` is passed **in** as an argument; the file defines the ramp, never the range.
- **`setFeatureState` must be re-applied on `sourcedata`, and that includes interaction state.** A GeoJSON source re-tiles whenever its data changes and drops all feature state with it. `MapCanvas` therefore writes the analytics values *and* `terpilih`/`hover` from a single `apply()` registered on `sourcedata`. An earlier version set `terpilih` from its own one-shot effect: the numbers came back after a re-tile but the selected ring silently vanished. Fase 2 hits the same hazard for a different reason (new vector tiles arriving on pan — ROADMAP 2.4).
- **Don't register map event listeners inside the effect that also sets `layersReady`.** That effect has `layersReady` both as a dependency and as an early-return guard, so it runs a second time the moment the flag flips: React tears down the old listeners first, then the guard returns before re-adding them. The map looks perfect and answers no clicks. Interaction handlers live in their own effect keyed on `layersReady`.
- **Project docs**: `DATA_CONTRACT.md` (frontend ⇄ backend agreement), `ROADMAP.md` (phased plan), and `USER_FLOW.md` (what a user can actually do, screen by screen — written in Obsidian flavour: YAML frontmatter, callouts, mermaid, and `[[wikilinks]]` to the other three docs) are committed. `USER_FLOW.md` deliberately separates working interactions from the intentionally-inert ones, so keep that split honest when editing it. `Proposal_IsiStasiun.pdf` and `BACKEND_TASK_DIVISION_3_PERSON.md` are gitignored — read them for context but never commit them.
- **Typed route props**: `app/layout.tsx` takes `LayoutProps<"/">` (see its `children` prop), a globally-available type Next.js 16 generates per-route from `.next/types` — not an import. Page components would use the equivalent `PageProps<"...">`. None of the 5 `page.tsx` files need it yet (no dynamic segments), but reach for `PageProps<"/route">` rather than hand-rolling a props type if one gains params/searchParams.
